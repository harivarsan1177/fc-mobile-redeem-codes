import type { CodeItem } from '../types/code';

/**
 * Empty seed data array.
 * Default dummy codes removed to ensure website only shows real codes.
 */
export const INITIAL_DEV_SEED_CODES: CodeItem[] = [];

